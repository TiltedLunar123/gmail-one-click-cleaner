{
  "name": "gmail-one-click-cleaner",
  "version": "7.4.0",
  "private": true,
  "description": "Free up Gmail storage fast. Bulk delete old emails, large attachments, & promotions.",
  "scripts": {
    "build": "node build.js",
    "build:firefox": "node build.js --target=firefox",
    "build:all": "node build.js --target=all",
    "build:prod": "node build.js --minify",
    "lint": "eslint *.js",
    "lint:firefox": "node build.js --target=firefox && addons-linter dist-firefox",
    "watch": "node build.js && echo Watching... && node -e \"require('fs').watch('.',{recursive:false},(e,f)=>{if(f&&!f.startsWith('dist')){console.log('Changed:',f);require('child_process').execFileSync('node',['build.js'],{stdio:'inherit'})}});setInterval(()=>{},86400000)\"",
    "zip": "node build.js --zip",
    "zip:all": "node build.js --zip --target=all",
    "clean": "node -e \"const fs=require('fs'); ['dist','dist-firefox','gmail-one-click-cleaner.zip','gmail-one-click-cleaner-firefox.zip'].forEach(f=>{try{fs.rmSync(f,{recursive:true})}catch{}})\"",
    "test": "jest --verbose",
    "test:ci": "jest --ci --coverage"
  },
  "devDependencies": {
    "addons-linter": "^10.7.0",
    "esbuild": "^0.28.1",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
